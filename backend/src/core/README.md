# Core

This directory preserves Mike-derived backend code that remains under AGPL-3.0.

- Files here are not mounted by the Juridisk v1 API unless explicitly wired in.
- Preserve AGPL notices and provenance when moving code into this directory.
